[pytest]
testpaths = tests
markers =
    known_inputs: deterministic parser, command, and registry fixtures
    lifecycle: connection and shutdown behavior with controlled transports
    container: Docker runtime permission validation
    tls: MQTT TLS integration coverage
    integration: tests that start local external processes such as Mosquitto
addopts = -ra
